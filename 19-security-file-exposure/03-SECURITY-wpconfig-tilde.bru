meta {
  name: "[SECURITY] WP Config Backup ~"
  type: http
  seq: 3
}

get {
  url: {{base_url}}/wp-config.php~
}

assert {
  res.status: in [403, 404]
}

tests {
  test("WP Config tilde backup must not be accessible", function() {
    expect(res.status).to.be.oneOf([403, 404]);
  });
}

docs {
  Αναμενόμενο: 403 ή 404
}
